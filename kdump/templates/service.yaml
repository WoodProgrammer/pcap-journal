apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdump.fullname" . }}
  labels:
    {{- include "kdump.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kdump.selectorLabels" . | nindent 4 }}
